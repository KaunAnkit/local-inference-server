[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "local-inference-server"
version = "0.1.0"
description = "A local LLM inference server built from scratch"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]